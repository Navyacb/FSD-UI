<%- include('includes/head.ejs') %>
    <script type="text/javascript">
        function validateDetails()
        {
            try {
            } catch(err)
            {
            }
            //alert("Inside");
            //return false;
        }
    </script>
</head>
<body class="container-fluid">
<%- include('includes/header.ejs') %>
<!-- <div class="row"> -->
    <div id="pageContent" class="pageContent">
        <div class="container" style="width: 600px;">
            <div class="row text-center" style="padding:20px">
            <div class="col-12 h1 logo">Login</div>
            </div>
            <% if (data.error) { %>
                <%= data.error %>
                <% } %>
            <div class="row text-center ">
                <div class="col-3"></div>
                <div class="col-7">
                <form id="form1" name="form1" method="POST" action='/users/login' class="text-left">
            
                    <div class="form-group">
                        <label for="loginId"><span class="req">* </span> User Name: </label> 
                            <input type="text" class="form-control" id="loginId" name="loginId" pattern="^[A-Za-z][A-Za-z0-9 -]+$" required="true" placeholder="Login Id"/>   
                                <div class="status" id="status"></div>
                    </div>
                    <div class="form-group">
                        <label for="passwd"><span class="req">* </span> Password: </label>
                            <input type="password" class="form-control" id="passwd" name="passwd" pattern="^[A-Za-z0-9#%$! -]+$" required="true" placeholder="Password"/> 
                     </div>
                     <button type="submit" onclick="validateDetails()" class="btn btm-lg btn-secondary ">Sign In</button>
                  <div class="etc-login-form">
                <p>Do not have an account? <a href="/users/register">Sign Up</a></p>
                </div>
                <!-- <div class="etc-login-form">
                <a href="#">Forgot your password?</a>
                </div> -->
            </form>
        </div>
        </div>
        </div>
        </div>
    </body>
</html>